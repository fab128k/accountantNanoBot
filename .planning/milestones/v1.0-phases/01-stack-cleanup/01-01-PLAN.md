---
phase: 01-stack-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - core/file_processors.py
  - rag/adapters/local_folder.py
autonomous: true
requirements:
  - STACK-01
  - STACK-02
  - STACK-03

must_haves:
  truths:
    - "requirements.txt contains no LangChain, SQLAlchemy, PyPDF2, or beautifulsoup4 entries"
    - "requirements.txt contains pypdf>=4.0.0 and sentence-transformers>=3.0.0"
    - "All Python files that previously imported PyPDF2 now import pypdf instead"
    - "python -c 'import app' succeeds with no import errors"
  artifacts:
    - path: "requirements.txt"
      provides: "Clean dependency list"
      contains: "pypdf>=4.0.0"
    - path: "core/file_processors.py"
      provides: "PDF extraction using pypdf"
      contains: "from pypdf import PdfReader"
    - path: "rag/adapters/local_folder.py"
      provides: "PDF loading using pypdf"
      contains: "from pypdf import PdfReader"
  key_links:
    - from: "core/file_processors.py"
      to: "pypdf"
      via: "import PdfReader"
      pattern: "from pypdf import PdfReader"
    - from: "rag/adapters/local_folder.py"
      to: "pypdf"
      via: "import PdfReader"
      pattern: "from pypdf import PdfReader"
---

<objective>
Clean up requirements.txt by removing dead dependencies (LangChain, langchain-ollama, SQLAlchemy, beautifulsoup4) and replacing deprecated PyPDF2 with pypdf. Update the two Python files that import PyPDF2 to import from pypdf instead.

Purpose: Establish a clean, minimal dependency set with no unused or deprecated packages before building new features.
Output: Updated requirements.txt, updated core/file_processors.py, updated rag/adapters/local_folder.py
</objective>

<execution_context>
@/home/admaiora/.claude/get-shit-done/workflows/execute-plan.md
@/home/admaiora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stack-cleanup/01-CONTEXT.md

<interfaces>
<!-- No new interfaces created. Existing import contract preserved: PdfReader API is identical between PyPDF2 and pypdf. -->

From core/file_processors.py (current, line 82):
```python
from PyPDF2 import PdfReader  # CHANGE TO: from pypdf import PdfReader
```

From rag/adapters/local_folder.py (current, line 183):
```python
from PyPDF2 import PdfReader  # CHANGE TO: from pypdf import PdfReader
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>requirements.txt</read_first>
  <action>
Edit requirements.txt to produce the following final content (preserving section comments):

```
# AccountantNanoBot v1.0.0
# Sistema contabile multi-agente con Ollama + RAG + Streamlit

# Core Framework
streamlit>=1.28.0
python-dotenv>=1.0.0
pyyaml>=6.0

# LLM Client (Ollama via OpenAI-compatible API)
openai>=1.0.0

# Knowledge Base / RAG
chromadb>=0.4.0
sentence-transformers>=3.0.0

# PDF Processing
pypdf>=4.0.0

# Export
reportlab>=4.0.0

# Document Processing
python-docx>=1.1.0
Pillow>=10.0.0

# XML Parser (FatturaPA)
lxml>=5.0.0

# Dev utilities
requests>=2.31.0
```

Specifically:
- DELETE line `langchain>=0.3.0` and its section comment `# Agent Orchestration`
- DELETE line `langchain-ollama>=0.2.0`
- DELETE line `sqlalchemy>=2.0.0` and its section comment `# Database`
- DELETE line `beautifulsoup4>=4.12.0`
- DELETE line `PyPDF2>=3.0.0`
- ADD line `sentence-transformers>=3.0.0` under the `# Knowledge Base / RAG` section
- ADD line `pypdf>=4.0.0` under a new `# PDF Processing` section
- UPDATE `python-docx>=0.8.0` to `python-docx>=1.1.0` (current stable)
  </action>
  <verify>
    <automated>grep -c "langchain\|sqlalchemy\|PyPDF2\|beautifulsoup4" requirements.txt | grep -q "^0$" && grep -q "pypdf>=4.0.0" requirements.txt && grep -q "sentence-transformers>=3.0.0" requirements.txt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- requirements.txt contains zero lines matching "langchain", "sqlalchemy", "PyPDF2", or "beautifulsoup4" (case-sensitive)
- requirements.txt contains the line `pypdf>=4.0.0`
- requirements.txt contains the line `sentence-transformers>=3.0.0`
- requirements.txt contains `chromadb>=0.4.0`
- requirements.txt contains `openai>=1.0.0`
- requirements.txt does NOT contain `# Agent Orchestration`
- requirements.txt does NOT contain `# Database`
  </acceptance_criteria>
  <done>requirements.txt has only the target dependencies with no dead or deprecated packages</done>
</task>

<task type="auto">
  <name>Task 2: Replace PyPDF2 imports with pypdf in Python files</name>
  <files>core/file_processors.py, rag/adapters/local_folder.py</files>
  <read_first>core/file_processors.py, rag/adapters/local_folder.py</read_first>
  <action>
In `core/file_processors.py`:
1. Line 82: Change `from PyPDF2 import PdfReader` to `from pypdf import PdfReader`
2. Line 98: Change the error message `return "[Errore: PyPDF2 non installato]"` to `return "[Errore: pypdf non installato. Installa con: pip install pypdf>=4.0.0]"`

In `rag/adapters/local_folder.py`:
1. Line 183: Change `from PyPDF2 import PdfReader` to `from pypdf import PdfReader`
2. Line 192: Change the error message `print("⚠️ PyPDF2 non installato. Installa con: pip install PyPDF2")` to `print("⚠️ pypdf non installato. Installa con: pip install pypdf>=4.0.0")`
3. Line 21 (docstring): Change `- File .pdf (PDF - richiede PyPDF2)` to `- File .pdf (PDF - richiede pypdf)`
4. Line 174 (docstring): Change `Richiede PyPDF2.` to `Richiede pypdf.`
  </action>
  <verify>
    <automated>grep -rn "PyPDF2" core/file_processors.py rag/adapters/local_folder.py | grep -v "^$" | wc -l | grep -q "^0$" && grep -q "from pypdf import PdfReader" core/file_processors.py && grep -q "from pypdf import PdfReader" rag/adapters/local_folder.py && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `core/file_processors.py` contains `from pypdf import PdfReader` (no occurrence of `PyPDF2`)
- `core/file_processors.py` contains the string `pypdf non installato`
- `rag/adapters/local_folder.py` contains `from pypdf import PdfReader` (no occurrence of `PyPDF2`)
- `rag/adapters/local_folder.py` contains the string `pypdf non installato`
- `rag/adapters/local_folder.py` docstring says `richiede pypdf` not `richiede PyPDF2`
- Zero occurrences of the string `PyPDF2` in either file (verified by grep)
  </acceptance_criteria>
  <done>Both Python files import from pypdf instead of PyPDF2, with updated error messages and docstrings</done>
</task>

</tasks>

<verification>
1. `grep -rn "PyPDF2\|langchain\|sqlalchemy\|beautifulsoup4" requirements.txt core/ rag/` returns zero matches from production code (planning docs excluded)
2. `grep -q "from pypdf import PdfReader" core/file_processors.py && echo OK` prints OK
3. `grep -q "from pypdf import PdfReader" rag/adapters/local_folder.py && echo OK` prints OK
4. `grep -q "sentence-transformers>=3.0.0" requirements.txt && echo OK` prints OK
</verification>

<success_criteria>
- requirements.txt contains exactly the target dependency set: no LangChain, no SQLAlchemy, no PyPDF2, no beautifulsoup4; includes pypdf and sentence-transformers
- Both Python files that previously used PyPDF2 now use pypdf with zero remaining PyPDF2 references
- No import errors introduced (verified by grep for correct import statements)
</success_criteria>

<output>
After completion, create `.planning/phases/01-stack-cleanup/01-01-SUMMARY.md`
</output>
